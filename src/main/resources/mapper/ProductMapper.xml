<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gzasc.wechatappwaimai.mapper.ProductMapper">

    <resultMap id="BaseResultMap" type="com.gzasc.wechatappwaimai.entity.Product">
        <id column="id" property="id"/>
        <result column="shop_id" property="shopId"/>
        <result column="name" property="name"/>
        <result column="image" property="image"/>
        <result column="price" property="price"/>
        <result column="description" property="description"/>
        <result column="status" property="status"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT id, shop_id, name, image, price, description, status, create_time, update_time
        FROM product
        WHERE id = #{id}
    </select>

    <update id="updateImage">
        UPDATE product
        SET image = #{image},
            update_time = NOW()
        WHERE id = #{id}
    </update>

</mapper>
